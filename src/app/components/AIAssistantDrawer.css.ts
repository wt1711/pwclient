.MembersDrawer {
  width: 268px;
  background: var(--bg-surface-extra-raised);
  flex-shrink: 0;
}

.MembersDrawerHeader {
  padding: 0 var(--sp-tight);
}

.MemberDrawerContentBase {
  position: relative;
  overflow: hidden;
}

.MemberDrawerContent {
  padding: var(--sp-normal) var(--sp-tight);
}

.DrawerGroup {
  margin-bottom: var(--sp-normal);
}

.DrawerInput {
  background-color: var(--bg-surface-raised);
}

.DrawerVirtualItem {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
} 